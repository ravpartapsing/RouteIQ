# `pnpm dev` — the deployed dev API. Copy to .env.development and fill in from:
#   terraform -chdir=infra/terraform/envs/dev output -raw api_url
VITE_API_BASE_URL=https://REPLACE.execute-api.us-east-1.amazonaws.com
